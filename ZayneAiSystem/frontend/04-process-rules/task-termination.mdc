---
description: 前端任务的终止条件清单：索引同步与 lint 通过是终止条件，不是善后步骤
alwaysApply: true
---

# 任务终止条件

任何涉及前端代码变更的任务，在宣布完成前必须逐项核对。
以下各项是**终止条件**——未满足则任务未完成，不是可以跳过的善后动作。

1. **lint 零 error**：运行项目 lint，边界规则（层级依赖、shared 禁触 store 等）全部通过。
   禁止用 `eslint-disable` 绕过边界规则；确实过不去说明设计有问题，回头改设计或上报用户。
2. **组件索引已同步**：本次新增/修改的组件在 `components.llms.txt` 中条目已更新，
   FACT 字段与源码一致（updated 日期、props 签名、version）。
3. **Store 索引已同步**：本次新增/修改的 store 在 `stores.llms.txt` 中条目已更新。
4. **结构合规**：新文件落位符合 `directory-spec.md`（一组件一文件夹、store 归属正确）。

向用户汇报时，如有任何一项未满足（例如 lint 环境不可用），必须明确说明哪项未完成及原因，
不得默默省略。
