---
description: 设计工作流：token 取值、模式套用、五态强制、交付前批评环自查
globs: ["**/*.module.less", "**/pages/**", "**/*.tsx"]
alwaysApply: false
---

# 设计工作流

## 动手前（必须）

1. 读 `patterns.llms.txt`：新页面/区块优先套用既有模式（含其五态标准处理）。
   无可套用 → 向用户轻量上报新模式草案（版式一句话 + 组件构成）后再动手。
2. 视觉决策一律向 `docs/design/reference.md` 选定的参照语言对齐，
   禁止自行发明新风格；参照没覆盖的场景，从参照的同类场景类推。

## 开发中

- 颜色/间距/字号/圆角/阴影/z-index 只准取 token 语义层的值（stylelint 会强制）。
- 每个异步数据视图显式实现五态，套用模式中记录的标准处理。
- 发现 token 刻度不够用（如需要新颜色档）→ 停下走 L5，禁止在业务样式里私加。

## 交付前：批评环自查（必须）

1. 对渲染结果/截图按 `design-checks.md` 的 8 项批评环清单逐项自查。
2. 当场能修的（对比度、目标尺寸、层级混乱）当场修；
   需要动 token 或模式的，上报而非硬改。
3. 自查结论写进任务汇报（哪几项过、哪几项修过、哪几项上报）。

## 收工时（任务终止条件，不可跳过）

1. stylelint 零 error（token 白名单、禁裸色值、禁 !important）。
2. 五态核对：本次每个异步视图逐一确认。
3. `patterns.llms.txt` 已同步：新模式登记、既有模式的 applied_in 回填。
