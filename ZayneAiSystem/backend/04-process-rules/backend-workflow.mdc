---
description: 后端工作流：契约先行、分层纪律、破坏性契约变更必过 L5
globs: ["**/server/**", "**/handler/**", "**/service/**", "**/repository/**"]
alwaysApply: false
---

# 后端工作流

## 动手前（必须）

1. 读 `api.llms.txt`：需要的端点是否已存在？存在则复用，形状不够用走变更流程。
2. 新端点：**先写契约条目（status: draft）再写实现**。
   契约与需求挂钩（feat 字段引用 FEAT-id），涉及新表则先走数据层工作流。
3. 改 stable 端点：先分级——
   - 加法变更（新端点、响应加字段、请求加可选字段）→ 轻量确认后动手；
   - 破坏性变更（删/改名/改类型/加必填字段）→ **停下走 L5**，
     按 consumers 字段呈报受影响方，获批后优先新旧并存 + deprecated 过渡。

## 开发中

- 分层纪律：逻辑只写在 service，数据访问只写在 repository，
  handler 只做解析/校验/翻译，跨业务只准 service 对 service（lint 会强制）。
- 错误一律用全局错误码表的码值，新码先登记进 `api.llms.txt` 头部 ERRORS 区。
- 实现与契约冲突时以契约为准；认为契约不合理，回到变更流程改契约，
  禁止代码先行、契约事后补。

## 收工时（任务终止条件，不可跳过）

1. `backend-checks.md` 逐项通过（分层 lint、跨业务边界、语言级 lint 零 error）。
2. `api.llms.txt` 条目与实现一致，新端点 status 已转 stable。
3. 本次涉及的每条 AC 有对应测试且通过（接口级测试对着契约写）。
4. 涉及表结构的部分已按数据层工作流完成（schema 索引同步、迁移已实际执行过）。
