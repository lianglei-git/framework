---
description: agent 编制工作流：何时派子 agent、任务书自包含、交付前单遍评审
alwaysApply: true
---

# Agent 编制工作流

## 派发子 agent 的判据（三条同时满足才派）

1. 任务边界清晰、可用一句话说清完成标准；
2. 与主线工作可并行，或体量大到会挤爆主对话上下文；
3. 提取类任务（公共组件/store）已获用户 L5 批准。
模糊任务、探索性任务不派——边界说不清的活留在主对话里做。

## 派发纪律

- 任务书按 `agent-roster.md` 的六字段模板写全（goal / inputs / constraints /
  deliverables / termination / non_goals），子 agent 看不到主对话，缺项不派。
- 派发前在 `delegations.llms.txt` 登记（status: running），收工回填 outcome；
  失败也留痕，失败史比成功史更有检索价值。
- 子 agent 的产出同样要过流水线闸门，不因"是子 agent 做的"豁免。

## 交付前评审（每次代码交付必经）

- 机械闸门（lint/测试/索引核对）全过之后，做单遍 review：
  输入为变更 diff + 关联 FEAT 文档，AC 是评审基准。
- 只找需要判断力的问题（逻辑、边界、设计），不重复 lint 的工作。
- must-fix 必须处理（修复或有据反驳）；与评审意见争议不下 → 上报人裁决。

## 禁止事项

- 禁止设立常驻检查类 agent（监察/回归/测试专职）——那些职能属于 CI。
- 禁止让任何 LLM 环节检查"规则合规性"（那是 lint 的事，见判断力之尺）。
- 禁止用连环子 agent 接力长任务——上下文每过一道边界损失一次，
  长任务优先在主对话内分阶段完成。
